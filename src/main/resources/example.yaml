#Setup the search - Populate the top 2 selects and enter the token value and wait for the auto complete to popup
a:
  operation: wait
  selector: id
  name: demographic_input
b:
  operation: screenshot
  type: file
  targetdir: /tmp/out.png
